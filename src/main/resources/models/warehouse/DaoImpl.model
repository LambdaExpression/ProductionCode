package com.model.manage.locked.dao;

import org.springframework.stereotype.Repository;

import com.model.manage.locked.bean.${tblNameUppercase};
import com.ruan.db.BaseDaoImpl;

/**
 * ${tblName}表 Dao实现类
 *
 * @Author ${user} ${date?string("yyyy年MM月dd日")}
 *
 */
@Repository("${tblName}")
public class ${tblNameUppercase}DaoImpl extends BaseDaoImpl<${tblNameUppercase}> implements ${tblNameUppercase}Dao {

}
